apiVersion: v1
kind: Service
metadata:
  name: ranking-service
  namespace: recsys
spec:
  selector:
    app: ranking
ports:
    - name: metrics       
      protocol: TCP
      port: 80            
      targetPort: 8000