apiVersion: v1
kind: Service
metadata:
  name: user-embedding-service
  namespace: recsys
spec:
  selector:
    app: user-embedding

  ports:
      - name: metrics      
        protocol: TCP
        port: 80            
        targetPort: 8000