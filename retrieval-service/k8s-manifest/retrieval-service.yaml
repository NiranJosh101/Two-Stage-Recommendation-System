apiVersion: v1
kind: Service
metadata:
  name: retrieval-service
  namespace: recsys
spec:
  selector:
    app: retrieval
  ports:
    - name: metrics       
      protocol: TCP
      port: 80            
      targetPort: 8000